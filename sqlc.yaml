version: "2"
sql:
  - engine: "sqlite"
    queries: "./store/queries"
    schema: "./store/migrations"
    gen:
      go:
        package: "store"
        out: "store"
        emit_pointers_for_null_types: true